class Player {
  constructor(playerName) {
    this.name = playerName;
    this.shotAttempts = 0;
  }

  boardShot(clickShot) {
    const missedMsg = "It's a miss.";
    const hitMsg = "It's a hit!";
    const alreadyHitMsg = "You hit this ship before.";
    const sunkMsg = "You sunk my ${shipName}";
    const allSunkMsg = "You sank all of my battleships in ${shotAttempts} attempts."
        
    this.gameBoard[x][y];
            
        let shipArray = ship.location; // find ship array locations
        let hits = ship.damage; // where hits are recorded
      
          // hits.push(1);
          console.log({clickShot});
        
    
  }
}






// Click function to update the top box with result of click

// document.querySelectorAll('.square').forEach((boardDiv) => {
//   boardDiv.addEventListener('click', () => {
//     console.log({boardDiv});

//     const 
//   })
// });

